.yui-g.box
  .yui-u.first
    %h3 View my:
    %ul
      %li=link_to 'Profile._, user_path(@user)  
      %li=link_to 'Blog._, user_posts_path(@user)
      %li=link_to 'Photos._, user_photos_path(@user)
      %li=link_to 'Clippings._, user_clippings_path(@user)
      %li=link_to 'Friends._, accepted_user_friendships_path(@user)

  .yui-u
    %h3 Manage my:
    %ul
      %li=link_to 'Profile._, edit_user_path(@user)
      %li=link_to 'Account settings._, edit_account_user_path(@user)
      %li=link_to 'Blog posts._, manage_user_posts_path(@user)                
      -if current_user.admin?
        %li=link_to 'Admin dashboard._, admin_dashboard_path

.box.alt
  %h3 Stats
  %ul
    %li="Last logged in: #{@user.last_login_at && @user.last_login_at.strftime("%m/%d/%y")}"
    %li= @user.created_at.strftime("Member since: %m/%d/%y")
    -unless @user.posts.empty?
      %li="#{@user.posts.count} blog posts."
    -unless @user.photos.empty?
      %li="#{@user.photos.count} photos."
    -unless @user.clippings.empty?
      %li="#{@user.clippings.count} clippings."
    -unless @user.comments.empty?
      %li="#{@user.posts.count} comments."
    -unless @user.accepted_friendships.empty?
      %li="#{@user.accepted_friendships.count} friends"
console.log("configuring " + $('edit_post'));
$('edit_post').observe("ajax:before", function () {
    console.log("Submitting");
    $('editbox_spinner').show();
    $.save_rte
    console.log("saved");
    return false;
});
console.log("Setting button " + $('cancelButton'));
$('cancelButton').observe('click', function () {
    EditForm.cancel();
    return false;
});

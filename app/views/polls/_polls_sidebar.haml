.clear
%br
.padded
  %h2= :popular_polls.l
  %ul
    -polls.each do |poll|
      %li
        =link_to "#{truncate(poll.question, 75)} <small class='quiet'>(#{:vote_amount.l :count => poll.votes.count})</small>", user_post_path(poll.post.user, poll.post)

<h1>Listing invitations</h1>

<table>
  <tr>
  </tr>
  
<% for invitation in @invitations %>
  <tr>
    <td><%= link_to 'Show._, user_invitation_path(invitation) %></td>
    <td><%= link_to 'Edit._, edit_user_invitation_path(invitation) %></td>
    <td><%= link_to 'Destroy._, user_invitation_path(invitation), :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New invitation', new_user_invitation_path %>
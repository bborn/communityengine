.yui-b
  = render :partial => 'shared/admin_nav'

#yui-main
  .yui-b.box
    %h3= "Ads".l
    %table{"width"=>"100%"}
      %tr
        %th="Name".l
        %th="Frequency".l
        %th="Published?".l
        %th="Run".l
        %th="Location".l

      - for ad in @ads
        %tr
          %td= link_to h(ad.name), ad_path(ad)
          %td= h ad.frequency
          %td= h ad.published?
          %td= h ad.time_constrained? ? "#{ad.start_date.to_formatted_s(:short)}-#{ad.end_date.to_formatted_s(:short)}" : "n/a"
          %td= h ad.location
          %td= link_to 'Show._, ad_path(ad)
          %td= link_to 'Edit._, edit_ad_path(ad)
          %td= link_to 'Destroy._, ad_path(ad), :confirm => 'Are you sure?', :method => :delete
            
    %br
    = link_to 'New Ad._, new_ad_path

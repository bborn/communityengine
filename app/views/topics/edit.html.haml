-@page_title= :edit_topic.l

%p
  =link_to :back.l, @forum, :class => 'btn'
  =link_to :show.l, [@forum, @topic], :class => 'btn'
  =link_to :delete.l, [@forum, @topic], :class => 'btn', :method => 'delete', :confirm => :are_you_sure.l, :class => 'btn btn-danger'

=render 'form'
